# event_loop.EventLoop { #dareplane_utils.general.event_loop.EventLoop }

```python
general.event_loop.EventLoop(self, dt_s, stop_event=Event(), ctx=None)
```



## Methods

| Name | Description |
| --- | --- |
| [validate_callback](#dareplane_utils.general.event_loop.EventLoop.validate_callback) | Check that every callback accepts at least a kwarg with 'ctx' |

### validate_callback { #dareplane_utils.general.event_loop.EventLoop.validate_callback }

```python
general.event_loop.EventLoop.validate_callback(cb)
```

Check that every callback accepts at least a kwarg with 'ctx'